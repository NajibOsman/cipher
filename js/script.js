var prompt_name=prompt("enter a sentence");


 function capitalizeFirstLetter (string) {
  return string && s [0].toUpperCase() + string.slice(1);
}
capitalizeFirstLetter(prompt_name);

function reverse(s){
  return s.split("").reverse().join("");
}

function callTo(capitalizeFirstLetter, reverse){
  return
}
